---
import BaseLayout from '../layouts/BaseLayout.astro';
import { contactLink, openSourceTools, tools } from '../data/siteContent';

const flagship = tools.find((tool) => tool.slug === 'cloud-cost-guard-lumen');
const signalAudit = tools.find((tool) => tool.slug === 'signal-audit');
const interactiveLab = tools.find((tool) => tool.slug === 'interactive-lab');
const marketTape = tools.find((tool) => tool.slug === 'market-tape');
const contactEmail = contactLink.href.replace(/^mailto:/, '');
---

<BaseLayout
  title="Work | Cloud & Capital"
  description="Tools for the decisions behind the bill. Explore Cloud & Capital systems for cloud, AI, and SaaS spend."
>
  <main id="main-content">
    <section class="work-opening" aria-labelledby="work-title">
      <div class="work-opening-grid">
        <div class="work-opening-text">
          <p class="section-label">Work</p>
          <h1 id="work-title">Tools for the decisions behind the bill.</h1>
          <p class="work-opening-copy">I build FinOps tools and decision systems for cloud, SaaS, and AI infrastructure spend. The work starts before the invoice, where architecture, commitments, and operating choices shape the bill.</p>
        </div>
        <div class="decision-diagram" aria-label="Cloud and Capital decision chain: Architecture, Commitments, and Utilization shape cost before the Invoice reports it.">
          <p class="diagram-group-label">Cost is shaped here</p>
          <div class="diagram-layers">
            <span class="diagram-connector-line" aria-hidden="true"></span>
            <span class="diagram-connector-dot" aria-hidden="true"></span>
            <span class="diagram-connector-dot" aria-hidden="true"></span>
            <span class="diagram-connector-dot" aria-hidden="true"></span>
            <div class="diagram-layer"><span>01</span><div><strong>Architecture</strong><small>compute · storage · data</small></div></div>
            <div class="diagram-layer"><span>02</span><div><strong>Commitments</strong><small>term · coverage · flexibility</small></div></div>
            <div class="diagram-layer"><span>03</span><div><strong>Utilization</strong><small>demand · idle · variance</small></div></div>
          </div>
          <div class="diagram-invoice">
            <span>04</span><div><strong>Invoice</strong><small>reported cost</small></div>
          </div>
        </div>
      </div>
    </section>

    {flagship && (
      <section class="flagship" aria-labelledby="flagship-title">
        <div class="flagship-intro">
          <p class="section-label section-label-dark">Flagship system</p>
        </div>
        <div class="flagship-grid">
          <div class="flagship-copy">
            <p class="tool-meta">{flagship.label}</p>
            <h2 id="flagship-title">Cloud Cost Guard <span>+ Lumen</span></h2>
            <p>{flagship.description}</p>
            <div class="flagship-actions">
              <a href={flagship.href} target="_blank" rel="noopener noreferrer" data-analytics-event="cta_click" data-analytics-destination="cloud_cost_guard" data-analytics-source="work_flagship">View live dashboard <span aria-hidden="true">→</span></a>
              <span>Cloud · AI · SaaS</span>
            </div>
          </div>
          <div class="flagship-visual" aria-label="Illustrative Cloud Cost Guard and Lumen analysis">
            <div class="lumen-panel work-rail-chat">
              <div class="work-rail-kicker">Lumen analysis</div>
              <div class="work-chat-bubble work-chat-bubble-user">What’s bleeding money right now?</div>
              <div class="work-chat-bubble work-chat-bubble-ai work-chat-bubble-ai-main">
                <strong>EC2 jumped 154% in one day, from $482 to $1,226.</strong>
                It is now the largest cost driver at <strong>40.5% of cloud spend.</strong><br /><br />
                I also found <strong>19 unused SaaS licenses costing $242 per month.</strong>
                AI spend rose <strong>12.4%</strong>, which is worth checking against token volume and model mix before it becomes the next material driver.<br /><br />
                <strong>Start with EC2, then validate the AI increase. Was this a planned launch or uncontrolled autoscaling?</strong>
              </div>
            </div>
            <div class="cost-shape-panel">
              <p class="visual-kicker">Current cost shape</p>
              <div class="donut" aria-hidden="true"></div>
              <p class="cost-shape-title">Tech spend by scope</p>
              <ul>
                <li><span class="dot cloud"></span><span>Cloud infrastructure</span><strong>88.1%</strong></li>
                <li><span class="dot saas"></span><span>SaaS tools</span><strong>7.6%</strong></li>
                <li><span class="dot ai"></span><span>AI / LLM</span><strong>4.3%</strong></li>
              </ul>
            </div>
          </div>
        </div>
      </section>
    )}

    <section class="interactive-tools" aria-labelledby="interactive-title">
      <div class="section-heading">
        <p class="section-label">Decision tools</p>
        <h2 id="interactive-title">Start with the system.<br />Then work the decision.</h2>
        <p>Use the tools separately or in sequence: diagnose where cost accountability breaks down, then turn a live technology-spend question into a decision-ready brief.</p>
      </div>
      <div class="decision-suite">
        {signalAudit && (
          <article class="decision-tool">
            <div class="tool-summary">
              <p class="tool-meta">01 · Assess the system</p>
              <h3>{signalAudit.title}</h3>
              <p>{signalAudit.description}</p>
            </div>
            <dl class="tool-facts">
              <div><dt>Input</dt><dd>12 guided questions</dd></div>
              <div><dt>Process</dt><dd>Decision-chain diagnostic</dd></div>
              <div><dt>Output</dt><dd>Weakest link + priority move</dd></div>
            </dl>
            <div class="tool-action">
              <p class="action-label">Leave with</p>
              <p class="tool-deliverable">Weakest link, operating pattern, and priority moves.</p>
              <a href={signalAudit.href} data-analytics-event="cta_click" data-analytics-destination="signal_audit" data-analytics-source="work_decision_tools">Take the Signal Audit <span aria-hidden="true">→</span></a>
            </div>
          </article>
        )}
        {interactiveLab && (
          <article class="decision-tool">
            <div class="tool-summary">
              <p class="tool-meta">02 · Work the decision</p>
              <h3>{interactiveLab.title}</h3>
              <p>{interactiveLab.description}</p>
            </div>
            <dl class="tool-facts">
              <div><dt>Input</dt><dd>Your decision</dd></div>
              <div><dt>Process</dt><dd>Guided context</dd></div>
              <div><dt>Output</dt><dd>Decision brief + next actions</dd></div>
            </dl>
            <div class="tool-action">
              <p class="action-label">Leave with</p>
              <p class="tool-deliverable">Compared options, explicit economics, and a decision-ready brief.</p>
              <a href={interactiveLab.href} data-analytics-event="cta_click" data-analytics-destination="interactive_lab" data-analytics-source="work_decision_tools">Build a Decision Brief <span aria-hidden="true">→</span></a>
            </div>
          </article>
        )}
      </div>
      {marketTape && (
        <article class="market-context">
          <div>
            <div class="market-identity">
              <p class="tool-meta">Market context</p>
              <h3>{marketTape.title}</h3>
            </div>
            <p>Live market signals shaping technology<br class="market-description-break" /> capital and infrastructure decisions.</p>
            <a href={marketTape.href} target="_blank" rel="noopener noreferrer" data-analytics-event="cta_click" data-analytics-destination="market_tape" data-analytics-source="work_market_context">Open Market Tape <span aria-hidden="true">→</span></a>
          </div>
        </article>
      )}
    </section>

    <section class="pipeline" aria-labelledby="pipeline-title">
      <div class="pipeline-heading">
        <p class="section-label">Open source pipeline</p>
        <h2 id="pipeline-title">The Six Tool Pipeline</h2>
        <p>Six open source FinOps tools spanning allocation, anomaly detection, resilience economics, AI spend, SaaS, and executive reporting.</p>
        <a href="https://github.com/cloudandcapital" target="_blank" rel="noopener noreferrer" data-analytics-event="cta_click" data-analytics-destination="github_pipeline" data-analytics-source="work_pipeline">View the pipeline on GitHub <span aria-hidden="true">→</span></a>
      </div>
      <ol class="pipeline-list">
        {openSourceTools.map((tool, index) => (
          <li>
            <span class="pipeline-number">{String(index + 1).padStart(2, '0')}</span>
            <div>
              <h3>{tool.title}</h3>
              <p>{tool.description}</p>
            </div>
            <a href={tool.href} target="_blank" rel="noopener noreferrer" aria-label={`View ${tool.title} on GitHub`} data-analytics-event="cta_click" data-analytics-destination="github_pipeline" data-analytics-source="work_pipeline">GitHub <span aria-hidden="true">→</span></a>
          </li>
        ))}
      </ol>
    </section>

    <section class="work-cta" aria-labelledby="work-cta-title">
      <div>
        <p class="section-label section-label-gold">Start a conversation</p>
        <h2 id="work-cta-title">Working on cloud economics, FinOps, or AI infrastructure?</h2>
      </div>
      <div class="work-cta-contact">
        <p>Bring the live decision, the deadline, and what is at stake.</p>
        <a href={contactLink.href} data-analytics-event="cta_click" data-analytics-destination="contact" data-analytics-source="work_contact">
          <span class="work-cta-link-label">Write directly</span>
          <span class="work-cta-email">{contactEmail}</span>
          <span class="work-cta-arrow" aria-hidden="true">→</span>
        </a>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  main { overflow: hidden; background: #f5eee9; }
  .work-opening, .interactive-tools, .pipeline { padding: clamp(4.75rem, 9vw, 8rem) clamp(1.5rem, 5vw, 4.5rem); }
  .work-opening { padding-top: clamp(6rem, 8vw, 7rem); padding-bottom: clamp(3.75rem, 5vw, 4.75rem); }
  .work-opening-grid { display: grid; grid-template-columns: minmax(0, 1.05fr) minmax(21rem, .95fr); gap: clamp(3rem, 8vw, 9rem); align-items: start; max-width: 88rem; }
  .section-label, .tool-meta { margin: 0 0 1.25rem; font-family: 'DM Mono', monospace; font-size: .62rem; letter-spacing: .18em; line-height: 1.5; text-transform: uppercase; color: #6b8e7f; }
  .section-label::after { content: ''; display: inline-block; width: 3rem; height: 1px; margin: 0 0 .25rem .75rem; background: currentColor; opacity: .55; }
  .work-opening h1, h2, h3 { font-family: 'Cormorant Garamond', Georgia, serif; font-weight: 300; color: #000; }
  .work-opening h1 { max-width: 14ch; margin: 0; font-size: clamp(3.1rem, 4.4vw, 4.75rem); line-height: .98; letter-spacing: -.035em; }
  .work-opening-copy { max-width: 58ch; margin: 2rem 0 0; font-family: 'DM Sans', sans-serif; font-size: clamp(.95rem, 1.4vw, 1.08rem); line-height: 1.82; color: #4a4a4a; }
  .decision-diagram { --diagram-pad: clamp(1.35rem, 3vw, 2.6rem); --diagram-connector: .65rem; --diagram-number: clamp(3.2rem, 5vw, 4.35rem); --diagram-gap: .85rem; min-height: 29rem; margin-top: 2.55rem; padding: clamp(1.5rem, 3vw, 2.5rem) 0 0; display: flex; flex-direction: column; background: #211f1b; color: #f5eee9; border-top: 1px solid #6b8e7f; }
  .diagram-group-label { margin: 0; padding: 0 var(--diagram-pad); font-family: 'DM Mono', monospace; font-size: clamp(.62rem, 1vw, .7rem); letter-spacing: .14em; line-height: 1.5; text-transform: uppercase; color: #a8c0b5; }
  .diagram-layers { display: grid; flex: 1; grid-template-columns: var(--diagram-connector) minmax(0, 1fr); grid-template-rows: repeat(3, minmax(5.5rem, auto)); column-gap: var(--diagram-gap); margin: 1.5rem 0 1.85rem; padding: 0 var(--diagram-pad); }
  .diagram-connector-line { grid-column: 1; grid-row: 1 / 4; justify-self: center; width: 1px; margin: 1.25rem 0; background: #6b8e7f; }
  .diagram-connector-dot { grid-column: 1; align-self: center; justify-self: center; width: 4px; height: 4px; border-radius: 50%; background: #86a89a; }
  .diagram-connector-dot:nth-of-type(2) { grid-row: 1; }.diagram-connector-dot:nth-of-type(3) { grid-row: 2; }.diagram-connector-dot:nth-of-type(4) { grid-row: 3; }
  .diagram-layer { display: grid; grid-column: 2; grid-template-columns: var(--diagram-number) minmax(0, 1fr); gap: var(--diagram-gap); align-items: center; min-height: 5.5rem; padding: 1.05rem 0; border-top: 1px solid rgba(245,238,233,.16); }
  .diagram-layer:nth-of-type(1) { grid-row: 1; }.diagram-layer:nth-of-type(2) { grid-row: 2; }.diagram-layer:nth-of-type(3) { grid-row: 3; }
  .diagram-layer:last-child { border-bottom: 1px solid rgba(245,238,233,.16); }
  .diagram-layer > span, .diagram-invoice > span { font-family: 'Cormorant Garamond', Georgia, serif; font-size: clamp(2.4rem, 4vw, 3.7rem); font-weight: 300; line-height: .8; color: #86a89a; }
  .diagram-layer strong, .diagram-invoice strong { display: block; font-family: 'Cormorant Garamond', Georgia, serif; font-size: clamp(1.45rem, 2.2vw, 2rem); font-weight: 400; line-height: 1; color: #f5eee9; }
  .diagram-layer small, .diagram-invoice small { display: block; margin-top: .45rem; font-family: 'DM Mono', monospace; font-size: .55rem; letter-spacing: .08em; line-height: 1.55; color: rgba(245,238,233,.6); }
  .diagram-invoice { display: grid; grid-template-columns: var(--diagram-connector) var(--diagram-number) minmax(0, 1fr); column-gap: var(--diagram-gap); align-items: center; padding: 1.5rem var(--diagram-pad); background: #ede5dc; border-top: 2px solid #86a89a; }
  .diagram-invoice > span { grid-column: 2; }.diagram-invoice > div { grid-column: 3; }
  .diagram-invoice > span { color: #6b8e7f; }.diagram-invoice strong { color: #000; }.diagram-invoice small { color: #4a4a4a; }
  .flagship { background: #000; color: #f5eee9; padding: clamp(3rem, 5vw, 4.75rem) clamp(1.5rem, 5vw, 4.5rem) clamp(4rem, 6vw, 5.5rem); border-top: 1px solid rgba(107,142,127,.5); }
  .flagship-intro { margin-bottom: 1.4rem; }
  .section-label-dark { color: rgba(245,238,233,.68); white-space: nowrap; }
  .flagship-grid { display: grid; grid-template-columns: minmax(0, .94fr) minmax(0, 1.06fr); border-top: 2px solid #6b8e7f; border-bottom: 1px solid rgba(245,238,233,.13); }
  .flagship-copy { min-height: 31rem; padding: clamp(2.25rem, 5vw, 4.4rem) clamp(1.5rem, 4vw, 3.75rem); display: flex; flex-direction: column; justify-content: center; border-right: 1px solid rgba(245,238,233,.13); }
  .flagship-copy .tool-meta { color: #c9a961; }
  .flagship-copy h2 { margin: 0 0 1.25rem; font-size: clamp(2.55rem, 4.5vw, 4.75rem); line-height: .98; color: #f5eee9; }
  .flagship-copy h2 span { display: block; }
  .flagship-copy > p:not(.tool-meta) { max-width: 39ch; margin: 0; font-family: 'DM Sans', sans-serif; font-size: .94rem; line-height: 1.85; color: rgba(245,238,233,.74); }
  .flagship-actions { display: flex; flex-wrap: wrap; gap: 1.25rem 1.5rem; align-items: center; margin-top: 2.25rem; }
  .flagship-actions a, .pipeline-heading > a, .pipeline-list a { font-family: 'DM Mono', monospace; font-size: .62rem; letter-spacing: .12em; line-height: 1.5; text-transform: uppercase; text-decoration: none; color: #6b8e7f; }
  .flagship-actions a { color: #86a89a; }
  .flagship-actions a > span[aria-hidden="true"], .tool-action a > span[aria-hidden="true"], .market-context a > span[aria-hidden="true"], .pipeline a > span[aria-hidden="true"] { margin-left: .45em; }
  .flagship-actions a:hover, .pipeline-heading > a:hover, .pipeline-list a:hover { text-decoration: underline; text-underline-offset: .3em; }
  .interactive-tools a:focus-visible { outline: 2px solid #3d6055; outline-offset: .35rem; }
  .flagship-actions span { font-family: 'DM Mono', monospace; font-size: .57rem; letter-spacing: .12em; text-transform: uppercase; color: rgba(245,238,233,.57); }
  .flagship-visual { display: grid; grid-template-columns: 1.1fr .9fr; overflow: hidden; border: 1px solid rgba(117, 96, 73, .3); border-radius: 20px; background: #f5eee9; transform: translateY(30px); }
  .lumen-panel, .cost-shape-panel { min-width: 0; padding: clamp(1.35rem, 2.4vw, 2rem); background: #f5eee9; color: #000; }
  .lumen-panel { display: flex; flex-direction: column; justify-content: center; }
  .visual-kicker { margin: 0 0 1.5rem; font-family: 'DM Mono', monospace; font-size: .56rem; letter-spacing: .14em; text-transform: uppercase; color: #6b8e7f; }
  .work-rail-chat { padding: 2.1rem 1rem 1.2rem 1.15rem; display: flex; flex-direction: column; }
  .work-rail-kicker { min-height: .8rem; margin-bottom: .95rem; font-family: 'DM Mono', monospace; font-size: .52rem; letter-spacing: .12em; text-transform: uppercase; color: #6f8f7a; }
  .work-chat-bubble { max-width: 90%; margin-bottom: .75rem; padding: .84rem .98rem; border-radius: 18px; font-family: 'DM Sans', sans-serif; font-size: .84rem; line-height: 1.72; }
  .work-chat-bubble-user { margin-left: auto; background: #b89a68; color: #fff; font-family: 'Cormorant Garamond', serif; font-size: 1.02rem; font-style: italic; line-height: 1.35; }
  .work-chat-bubble-ai { background: rgba(25,23,20,.06); color: #514b42; }
  .work-chat-bubble-ai-main { max-width: 100%; font-size: .79rem; line-height: 1.62; }
  .work-chat-bubble-ai-main strong { color: #1d1b18; font-weight: 700; }
  .cost-shape-panel { display: flex; flex-direction: column; border-left: 1px solid rgba(117, 96, 73, .18); }
  .donut { width: min(11rem, 100%); aspect-ratio: 1; margin: .25rem auto 1rem; border-radius: 50%; background: conic-gradient(#6b8e7f 0 88.1%, #a7b6d8 88.1% 95.7%, #c9a961 95.7% 100%); position: relative; }
  .donut::after { content: ''; position: absolute; inset: 23%; border-radius: 50%; background: #f5eee9; }
  .cost-shape-title { margin: .25rem 0 1rem; font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.25rem; line-height: 1.1; }
  .cost-shape-panel ul { list-style: none; margin: auto 0 0; padding: 0; }
  .cost-shape-panel li { display: grid; grid-template-columns: auto minmax(0, 1fr) auto; gap: .45rem; align-items: center; padding: .52rem 0; border-top: 1px solid rgba(0,0,0,.08); font-family: 'DM Mono', monospace; font-size: .54rem; letter-spacing: .04em; line-height: 1.35; color: #4a4a4a; }
  .cost-shape-panel strong { color: #000; font-weight: 400; }
  .dot { width: .42rem; height: .42rem; border-radius: 50%; }
  .dot.cloud { background: #6b8e7f; }.dot.saas { background: #a7b6d8; }.dot.ai { background: #c9a961; }
  .interactive-tools { --tool-grid-columns: minmax(20rem, 1.35fr) minmax(13rem, .8fr) minmax(17rem, 1.05fr); --tool-grid-gap: clamp(2rem, 4vw, 4.5rem); --tool-grid-inset: clamp(1.75rem, 2.6vw, 2.4rem); padding: clamp(2.6rem, 4vw, 3.75rem) clamp(1.5rem, 5vw, 4.5rem) clamp(2.4rem, 3.5vw, 3.25rem); background: #fbfaf7; border-bottom: 1px solid rgba(0,0,0,.11); }
  .section-heading, .decision-suite, .market-context { max-width: 88rem; margin-inline: auto; }
  .section-heading { display: grid; grid-template-columns: minmax(0, 1.08fr) minmax(20rem, .92fr); column-gap: clamp(2.5rem, 8vw, 8rem); row-gap: .7rem; align-items: center; margin-bottom: clamp(2rem, 2.75vw, 2.5rem); }
  .section-heading .section-label { grid-column: 1 / -1; margin-bottom: 0; }
  .section-heading h2, .pipeline-heading h2, .work-cta h2 { margin: 0; font-size: clamp(2.25rem, 4vw, 4rem); line-height: 1.03; letter-spacing: -.025em; }
  .section-heading > p:last-child { max-width: 37ch; margin: 0; padding: .45rem 0 .45rem clamp(1.5rem, 3vw, 3rem); border-left: 2px solid rgba(107,142,127,.4); font-family: 'Cormorant Garamond', Georgia, serif; font-size: clamp(1.15rem, 1.55vw, 1.4rem); font-style: italic; line-height: 1.45; letter-spacing: -.01em; color: #292929; }
  .decision-suite { border-top: 2px solid #6b8e7f; border-bottom: 1px solid rgba(0,0,0,.14); }
  .decision-tool { display: grid; grid-template-columns: var(--tool-grid-columns); gap: var(--tool-grid-gap); align-items: start; padding: var(--tool-grid-inset); background: #fff; }
  .decision-tool + .decision-tool { border-top: 1px solid rgba(0,0,0,.12); background: #edf3ee; }
  .tool-summary .tool-meta { margin: 0 0 .75rem; color: #4f7567; }
  .tool-summary h3, .market-context h3 { margin: 0; font-size: clamp(2.15rem, 2.75vw, 3rem); line-height: 1; letter-spacing: -.025em; }
  .tool-summary > p:not(.tool-meta), .market-context > div > p { margin: .9rem 0 0; font-family: 'DM Sans', sans-serif; font-size: .88rem; line-height: 1.68; color: #4a4a4a; }
  .tool-summary > p:not(.tool-meta) { max-width: 46ch; }
  .tool-facts { margin: 0; }
  .tool-facts div { padding: .7rem 0; border-top: 1px solid rgba(0,0,0,.1); }.tool-facts div:last-child { border-bottom: 1px solid rgba(0,0,0,.1); }
  .tool-facts dt { font-family: 'DM Mono', monospace; font-size: .55rem; letter-spacing: .13em; line-height: 1.4; text-transform: uppercase; color: #6b8e7f; }
  .tool-facts dd { margin: .24rem 0 0; font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.05rem; font-weight: 500; line-height: 1.28; letter-spacing: -.005em; color: #292929; }
  .tool-action { display: flex; flex-direction: column; }
  .tool-action .action-label { margin: 0 0 .45rem; font-family: 'DM Mono', monospace; font-size: .55rem; letter-spacing: .13em; line-height: 1.4; text-transform: uppercase; color: #6b8e7f; }
  .tool-action .tool-deliverable { max-width: 28ch; margin: 0; font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.28rem; line-height: 1.24; color: #292929; }
  .tool-action a, .market-context a { display: inline-flex; min-height: 44px; align-self: flex-start; align-items: center; justify-content: center; margin-top: 1.35rem; padding: .75rem 1rem; border: 1px solid #6b8e7f; font-family: 'DM Mono', monospace; font-size: .61rem; letter-spacing: .12em; line-height: 1.5; text-transform: uppercase; text-decoration: none; color: #3d6055; transition: background .18s, color .18s; }
  .tool-action a:hover, .market-context a:hover { background: #3d6055; color: #fff; }
  .market-context { margin-top: 1.25rem; padding: 1.35rem var(--tool-grid-inset); background: #f7f5f1; border-top: 1px solid rgba(0,0,0,.12); border-bottom: 1px solid rgba(0,0,0,.12); }
  .market-context > div { display: grid; grid-template-columns: var(--tool-grid-columns); gap: var(--tool-grid-gap); align-items: center; }
  .market-identity .tool-meta { margin: 0 0 .45rem; color: #8a7044; }.market-context h3 { font-size: clamp(1.7rem, 2.2vw, 2.25rem); }
  .market-context > div > p { margin: 0; }.market-context a { justify-self: start; align-self: center; margin-top: 0; white-space: nowrap; }
  .interactive-tools + .pipeline { padding-top: clamp(3rem, 5vw, 4.5rem); }
  .pipeline { padding-top: clamp(4rem, 7vw, 6.25rem); padding-bottom: clamp(3.5rem, 5vw, 4.75rem); display: grid; grid-template-columns: minmax(15rem, .7fr) minmax(0, 1.3fr); gap: clamp(2.5rem, 8vw, 8rem); background: #19241f; color: #f5eee9; border-top: 1px solid rgba(168,192,181,.28); }
  .pipeline .section-label { color: #b8cec3; }
  .pipeline-heading h2, .pipeline-list h3 { color: #f5eee9; }
  .pipeline-heading > p:not(.section-label) { max-width: 31ch; margin: 1.4rem 0 2rem; font-family: 'DM Sans', sans-serif; font-size: .9rem; line-height: 1.8; color: rgba(245,238,233,.72); }
  .pipeline-heading > a, .pipeline-list a { color: #b8cec3; }
  .pipeline-list { margin: 0; padding: 0; list-style: none; border-top: 1px solid rgba(245,238,233,.23); }
  .pipeline-list li { display: grid; grid-template-columns: 3.25rem minmax(0, 1fr) auto; gap: 1rem; align-items: baseline; padding: 1.45rem 0; border-bottom: 1px solid rgba(245,238,233,.23); }
  .pipeline-number { font-family: 'DM Mono', monospace; font-size: .59rem; letter-spacing: .08em; color: #d4b16e; }
  .pipeline-list h3 { margin: 0 0 .35rem; font-size: clamp(1.25rem, 2vw, 1.62rem); line-height: 1.08; }
  .pipeline-list p { margin: 0; font-family: 'DM Sans', sans-serif; font-size: .83rem; line-height: 1.65; color: rgba(245,238,233,.72); }
  .pipeline-list a { white-space: nowrap; }
  .pipeline a:focus-visible { outline: 2px solid #f5eee9; outline-offset: .35rem; }
  .pipeline + .work-cta { border-top: 1px solid rgba(221,185,119,.55); }
  .work-cta { display: grid; grid-template-columns: minmax(0, 1fr) minmax(19rem, .55fr); gap: clamp(3rem, 8vw, 8rem); align-items: center; padding: clamp(3.5rem, 7vw, 6rem) clamp(1.5rem, 5vw, 4.5rem); background: #3d6055; }
  .section-label-gold { color: #ddb977; }.section-label-gold::after { opacity: .45; }
  .work-cta h2 { max-width: 23ch; color: #fff; }
  .work-cta-contact { align-self: stretch; display: flex; flex-direction: column; justify-content: center; padding: 1.2rem 0; border-top: 1px solid rgba(255,255,255,.28); border-bottom: 1px solid rgba(255,255,255,.28); }
  .work-cta-contact > p { max-width: 30ch; margin: 0 0 1.15rem; font-family: 'Cormorant Garamond', Georgia, serif; font-size: 1.22rem; font-style: italic; line-height: 1.38; color: rgba(255,255,255,.84); }
  .work-cta-contact > a { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: .25rem 1rem; align-items: center; padding: .85rem 0 0; border-top: 1px solid rgba(255,255,255,.16); color: #fff; text-decoration: none; }
  .work-cta-link-label { font-family: 'DM Mono', monospace; font-size: .58rem; letter-spacing: .14em; line-height: 1.4; text-transform: uppercase; color: #ddb977; }
  .work-cta-email { grid-column: 1; font-family: 'Cormorant Garamond', Georgia, serif; font-size: clamp(1.15rem, 1.7vw, 1.45rem); line-height: 1.25; overflow-wrap: anywhere; }
  .work-cta-arrow { grid-column: 2; grid-row: 1 / 3; align-self: center; font-family: 'DM Mono', monospace; color: #ddb977; transition: transform .18s; }
  .work-cta-contact > a:hover .work-cta-arrow { transform: translateX(.3rem); }
  .work-cta-contact > a:focus-visible { outline: 2px solid #fff; outline-offset: .45rem; }
  @media (max-width: 920px) {
    .work-opening-grid, .flagship-grid, .pipeline { grid-template-columns: 1fr; }
    .work-opening-grid { gap: 0; }.work-opening h1 { max-width: 13ch; }
    .decision-diagram { width: min(100%, 42rem); margin-top: 3rem; }
    .flagship-copy { min-height: auto; border-right: none; border-bottom: 1px solid rgba(245,238,233,.13); }
    .flagship-visual { min-height: 30rem; }.pipeline { gap: 3rem; }
    .section-heading { grid-template-columns: 1fr; gap: 1.5rem; }.section-heading .section-label { grid-column: 1; margin-bottom: 0; }.section-heading > p:last-child { max-width: 58ch; padding-left: 1.5rem; }
    .decision-tool { grid-template-columns: minmax(0, 1.2fr) minmax(13rem, .8fr); }.tool-action { grid-column: 1 / -1; min-height: 0; }.tool-action a { margin-top: 1.15rem; }
    .market-context > div { grid-template-columns: minmax(12rem, .75fr) minmax(0, 1.25fr); }.market-context a { grid-column: 2; }
  }
  @media (max-width: 720px) {
    .flagship-intro { display: block; }.flagship-intro > p:last-child { margin-top: 1rem; }
    .flagship-visual { grid-template-columns: 1fr; min-height: 0; transform: translateY(22px); }.lumen-panel { min-height: 18rem; }.cost-shape-panel { border-top: 1px solid rgba(117, 96, 73, .18); border-left: none; }
    .decision-diagram { min-height: 26rem; }.diagram-layers { grid-template-rows: repeat(3, minmax(4.65rem, auto)); margin-top: 1rem; margin-bottom: 1.35rem; }.diagram-layer { min-height: 4.65rem; }
    .decision-tool { grid-template-columns: 1fr; gap: 1.4rem; padding: 1.75rem; }.tool-action { grid-column: 1; display: flex; gap: 0; align-items: stretch; }.tool-action a { width: 100%; margin-top: .9rem; }
    .market-context > div { grid-template-columns: 1fr; gap: .8rem; }.market-context a { grid-column: 1; width: 100%; margin-top: .3rem; }.market-description-break { display: none; }
    .work-cta { grid-template-columns: 1fr; align-items: start; }.work-cta-contact { width: 100%; }
  }
  @media (max-width: 480px) {
    .work-opening, .interactive-tools, .pipeline { padding-left: 1.25rem; padding-right: 1.25rem; }
    .work-opening h1 { font-size: clamp(2.9rem, 15vw, 4rem); }.flagship { padding-left: 1.25rem; padding-right: 1.25rem; }
    .flagship-copy { padding: 2.5rem 1.25rem; }.flagship-visual { padding: .65rem; }.pipeline-list li { grid-template-columns: 2.6rem minmax(0, 1fr); gap: .75rem; }.pipeline-list a { grid-column: 2; justify-self: start; }
    .section-heading > p:last-child { padding-left: 1rem; }.market-context { margin-inline: 0; }
    .work-cta { padding-left: 1.25rem; padding-right: 1.25rem; }
  }
</style>
